section.slider.content-section
  
  ul#image-slider
    
    //- Loop through the slider items
    each sliderItem in slice.value
      
      - var imageUrl = sliderItem.image ? sliderItem.image.url : ""
      
      li
        img(src=imageUrl)
